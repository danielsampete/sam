<%= form_for(feed_item.favrelationships.find_by_favby_id(current_user), remote: true, user: current_user, 
             html: { method: :delete }) do |f| %>
    <%= f.submit "Unlike", class: "btn btn-small btn-danger" %>
    <% end %>